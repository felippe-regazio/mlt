import styled from '@emotion/styled';

export const Form = styled.form`
  .field {
    input {
      display: block;
      width: 100%;
      border-color: #bbb;
    }
  }

  .actions {
    display: flex;
    gap: 8px;
  }
`;
